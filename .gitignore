# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Inno Setup 통짜 installer 출력물 (대용량 setup.exe — 루트 생성)
installer/Output/
/EngramOverlay_*_x64-setup.exe

# 오프라인 임베딩 모델 export (빌드 산출물, ~458MB — build-installer.ps1 이 생성)
resource/embedding-model/
!resource/embedding-model/
resource/embedding-model/*
!resource/embedding-model/manifest.json

# IDE
.vscode/
.idea/
*.swp
*.swo

ProjectIntelContunuum.code-workspace

# Environment
.env
*.env.local

# Database (stored externally at D:\intel_continuum\)
*.db
*.db-wal
*.db-shm

# OS
Thumbs.db
Desktop.ini
.DS_Store
NUL

# Test artifacts
_test_*.py
.tmp/

# Logs
overlay_debug.log
logs/

# User-specific configuration
overlay.user.yaml

# Binary files
*.pptx

# GUI artifacts
gui/node_modules

# build artifacts
resource/overlay.png
.mcp.json

# Release artifacts (GitHub Release 로 배포 — repo 에 넣지 않는다)
AMBER_*_x64-setup.exe
AMBER_*_ModelPack.zip
debug.log
# build-installer.ps1 이 핀 버전 wheel 을 여기로 스테이징한다. 빌드 산출물이므로
# 저장소에 담지 않는다 — 버전은 installer/external-overlay.pin 이 말한다.
installer/external-overlay.whl
installer/external-components/
