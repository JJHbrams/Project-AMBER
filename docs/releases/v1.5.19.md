# AMBER v1.5.19

## Release identity

- Engram source: `d171c73` (`prepare AMBER 1.5.19 release`)
- Four-part version: `1.5.19.770`
- Installer: `AMBER_1.5.19.770_x64-setup.exe`
- Size: `214,884,433` bytes
- SHA-256: `8BC187348A77126E1D1086B2F0BE3C3B86BF430B0E71DD025845F8FF74896FA4`

## Highlights

- Bubble bootstrap correctly identifies Codex and removes incompatible
  bootstrap instruction overlap.
- Native bubbles restore saved geometry, calculate DPI-aware tail positions,
  and close only a genuinely empty idle composer.
- The installer carries the complete Codex hook and connection-recovery
  sources without changing the user's hook-trust choice.
- Sage escalation is included through provider-owned Claude, Codex, and
  Copilot definitions.

## Verification

- Focused source tests cover bootstrap, bubble state and title recovery, Codex
  hook provisioning, native host/nudge/shell behavior, and Sage definitions.
- The native bubble frontend test suite passes.
- The frozen EXE and Inno installer both report ProductVersion `1.5.19.770`;
  the frozen build manifest records source `d171c73`.
- The frozen and installer build passed. The isolated full-suite candidate
  result was 553 passed, 10 failed, 1 skipped, and 1 deselected. The same ten
  test IDs also failed against source 1.5.18 baseline `25a6d14` under the same
  isolation; this comparison does not establish identical failure causes.

## Remaining runtime checks

The installer is built from Engram source `d171c73`. No disposable-profile,
setup-installed runtime smoke was performed. Real provider connection,
image/approval flows, and multi-DPI Windows visual behavior remain UNVERIFIED
in the frozen release runtime.
